# Standard Operating Procedure (SOP): Autonomous Storyboard Generation & Curation Pipeline

## 1. Objective
To autonomously generate, evaluate, and curate **334 production-ready storyboard images** for the video script using **Google Flow (Nano Banana Pro model priority, 16:9 widescreen, 2 variations per prompt)**, ensuring full-bleed composition, consistent ink explainer visual language with white-filled black-outlined stick figure characters, and zero manual intervention under strict fault-tolerant safety rules.

---

## 2. Model & Batch Configuration (Updated)
- **Primary Model**: `🍌 Nano Banana Pro` (Priority 1)
- **Model Priority Cascade**: `Nano Banana Pro > Nano Banana 2 > Nano Banana 2 Lite`
- **Variations per Prompt**: `x2` (optimized for generation speed and reliability)
- **Aspect Ratio**: `16:9` widescreen (1376 × 768 native render)
- **Pacing Interval**: `10 to 30 seconds` random delay between completed generations

---

## 3. Directory Architecture & File Mapping

| Directory / File | Path | Description |
| :--- | :--- | :--- |
| **Final Selected Images** | `Final selected images/` | **The single canonical destination folder** for all curated best images (`shot_001.jpg` ... `shot_334.jpg`). Legacy folder `final_selected_images` is deprecated and excluded. |
| **Raw Generated Variations** | `3- Finals/flow_generated_images/` | The raw image variations generated by Google Flow for each shot. |
| **Master Storyboard DB** | `3- Finals/storyboard_master.csv` | 5-column database with exact timing, voiceover, descriptions, and stick-figure prompts. |
| **Prompt Status Tracker** | `3- Finals/PROMPT_STATUS.md` & `PROMPT_STATUS.md` | Real-time visual dashboard showing completed vs. pending shots. |
| **All Prompts Book** | `3- Finals/all_prompts.txt` | Complete prompts with live `[STATUS: COMPLETED]` / `[STATUS: PENDING]` tags. |
| **Selection Log** | `3- Finals/selection_log.csv` | Historical audit log recording timestamp, winning variation, and vision scores. |
| **Live Pipeline Status** | `3- Finals/production_status.json` | JSON status object read by the monitoring engine. |

---

## 4. Step-by-Step Execution Protocol (Per Prompt)

For each Shot $N$ from 1 to 334:

### Step 1: Pre-Execution Verification
- Check if `Final selected images/shot_{N:03d}.jpg` already exists.
- If it exists, mark status as `[COMPLETED]`, skip generation, and immediately advance to Shot $N+1$.

### Step 2: Prompt Submission in Google Flow
- Focus the `.ProseMirror` editor on the active Google Flow project tab via Chrome CDP (port 9222).
- Inject the exact full-bleed prompt for Shot $N$.
- Verify model configuration is set to active model in cascade (`Nano Banana 2` / `Pro` / `2 Lite`) and `x2` variations.
- Click `Start generation` (`button[aria-label="Start generation"]`).

### Step 3: Render Monitoring, Partial Ingestion & Failsafe Cascade
- Continuously poll the DOM every 2.5 seconds (up to 50 seconds max wait).
- **Error Trigger Check**: Scan DOM for error banners, toasts, or text matching:
  - `"failed generation"`, `"something went wrong"`, `"unusual activity"`, `"generation failed"`, `"try again later"`, `"reached your usage limit"`.
- **Partial Generation Usability Check (1-Image Rule)**:
  - If a full batch (2 variations) did not complete or an error occurred, BUT at least 1 image was rendered (`len(new_srcs) >= 1`):
    - Download and evaluate that single image using computer vision scoring.
    - If `score >= 50.0` (usable): Accept it immediately as the final image for Shot $N$, save to final folders, reset failure counters, and advance to Step 5!
    - If `score < 50.0` (unusable): Reject the image and proceed to failure cooldown.
- **Escalating Failure Intervals & Model Cascade**:
  1. **Failure #1 on Shot $N$**:
     - Reload page (`flow_page.reload()`).
     - Wait **3 minutes (180 seconds)** cooldown.
     - Retry Shot $N$ with current model.
  2. **Failure #2 on Shot $N$**:
     - Reload page (`flow_page.reload()`).
     - Wait **5 minutes (300 seconds)** cooldown.
     - Retry Shot $N$ with current model.
  3. **Failure #3 on Shot $N$**:
     - Do NOT halt yet!
     - Shift model to the next model in cascade: `Nano Banana Pro` → `Nano Banana 2` → `Nano Banana 2 Lite`.
     - Reset consecutive failure counter to 0 for the new model.
     - Wait 30 seconds for UI stabilization, re-verify `x2` setting, and retry Shot $N$.
   4. **Exhaustion Halt**:
     - If all models in the cascade fail 3 times on the same shot:
       - Save full-page error screenshot (`2- Code/flow_error_halt.png`).
       - Update `production_status.json` with status `"HALTED_CONSECUTIVE_FAILURES"`.
       - **HALT PIPELINE and prompt the user.**

### Step 4: Asset Ingestion & Multi-Metric Computer Vision Scoring
- Once image variations render successfully:
  - Reset `consecutive_failures = 0`.
  - Download variations at native 1376×768 resolution to `3- Finals/flow_generated_images/shot_{N:03d}_var_{1..2}.jpg`.
  - Run multi-metric image evaluation:
    1. **Sharpness & Line Art Clarity (30% weight)**: Laplacian edge variance scoring crisp black ink contours.
    2. **Subject Prominence & Center Focus (25% weight)**: Contrast distribution focusing on primary subject.
    3. **Tonal Dynamic Range (20% weight)**: Percentile value distribution ensuring ink depth without blown highlights.
    4. **Information Entropy (15% weight)**: Detail density and narrative clarity.
    5. **Stick Figure Character Verification (10% weight / Penalty)**: For character scenes, detect stick-figure compliance. Heavy penalty (-40 points) if realistic human anatomy, flesh tones, or detailed faces are detected instead of white-filled black-outlined stick figures.
    6. **Full-Bleed Penalty**: Automatic -50 point penalty if solid white card border/matting is detected.

### Step 5: Final Selection, Archival, Pacing & Status Tagging
- Identify the highest-scoring variation.
- Copy winning variation strictly to:
  - `Final selected images/shot_{N:03d}.jpg` (The single destination folder; no other destination folders or deprecated legacy paths).
- Append record to `3- Finals/selection_log.csv`.
- Update `[STATUS: COMPLETED]` in `3- Finals/all_prompts.txt`.
- Update progress metrics in `PROMPT_STATUS.md` and `production_status.json`.
- **Random Pacing Delay**: Wait a randomized interval between **10 and 30 seconds** before beginning the next prompt to maintain natural traffic patterns.
- Advance to Shot $N+1$.

---

## 5. Quality Standard
- **Resolution**: 1376 × 768 native 16:9 widescreen.
- **Character Standard (Mandatory)**: All human characters MUST be minimalist white-filled black-outlined stick figures (MinutePhysics / Casually Explained style) with simple circular heads, pure solid white head fill, clean bold black comic outlines, simple black stick limbs, zero realistic human anatomy, and zero flesh skin tones.
- **Style Consistency**: Minimalist 2D vector comic linework, bold black ink outlines, flat muted colors, edge-to-edge environment grounding.
- **Zero Border Policy**: 100% full bleed. No floating cards, no white matting borders.
- **Destination Folder**: All final approved images reside exclusively in `Final selected images/`. Deprecated folder `final_selected_images` is forbidden.

---

## 6. Voiceover (VO) Production Standards (ElevenLabs)
- **Generation Mode**: **ALWAYS COMBINED**. Never generate voiceovers sentence-by-sentence. All script narrations must be passed as unified continuous paragraphs/sections to ensure natural human cadence, seamless emotional inflection, and natural breath pacing.
- **Active Voice**: `Tyler - Clear US YouTube Creator Voice` (Voice ID: `rPMkKgdwgIwqv4fXgR6N`).
- **Active Model**: `eleven_multilingual_v2` (SOTA emotional stability and high fidelity).
- **Silence Normalization Standard**: If an inter-sentence silence pause is greater than 300ms, the excess silence is removed, preserving exactly **150ms tail silence after the current sentence** and **150ms head silence before the next sentence** (total 300ms natural cushion). Timestamps for subsequent sentences and words are sample-accurately adjusted.
- **NLE XML Sequence Assembly**: An Apple `xmeml` XML sequence (`storyboard_timeline.xml`) places the master normalized audio on Audio Track 1 and maps every storyboard image to Video Track 1 aligned to its exact voiceover sentence timing with zero black gaps.
- **Target Directory**: `3- Finals/[Video Title]/Voiceovers/`.
- **Environment Source**: All API credentials, voice IDs, and audio settings are strictly sourced from `.env`.
